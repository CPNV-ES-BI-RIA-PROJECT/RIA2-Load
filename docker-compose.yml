services:
  app:
    build: .
    image: load:latest
    ports:
      - "${SERVER_PORT:-8080}:${SERVER_PORT:-8080}"
    env_file:
      - .env
    environment:
      SERVER_PORT: ${SERVER_PORT:-8080}
      BUCKET_ADAPTER_BASE_URL: ${BUCKET_ADAPTER_BASE_URL}
